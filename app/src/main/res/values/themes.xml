<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The window is black before Compose draws anything, so launching the app never shows a pale
      flash. windowLightStatusBar is false so the system bars' own icons stay light on the black.
    -->
    <style name="Theme.SamplePlayer" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
